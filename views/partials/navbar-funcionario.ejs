<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
    <div class="container-fluid">
        <a class="navbar-brand" href="/funcionario/dashboard">
            <i class="bi bi-shop me-2"></i>
            Claudinha Store PDV
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarFuncionario">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarFuncionario">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link <%= caixa ? '' : 'disabled' %>" href="/funcionario/vendas/new">
                        <i class="bi bi-cart-plus-fill me-1"></i> Nova Venda
                    </a>
                </li>
                </ul>

            <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-center">
                
                <li class="nav-item me-3">
                    <% if (caixa) { %>
                        <span class="navbar-text">
                            <span class="badge bg-success me-2">Caixa Aberto</span>
                            PDV: <strong><%= caixa.pdv.identificacao %></strong>
                        </span>
                    <% } else { %>
                         <span class="navbar-text">
                            <span class="badge bg-danger me-2">Caixa Fechado</span>
                        </span>
                    <% } %>
                </li>

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                        <i class="bi bi-person-circle me-1"></i>
                        <%= user.nome %>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end dropdown-menu-dark">
                        <li>
                            <form action="/logout" method="POST" class="d-inline">
                                <button type="submit" class="dropdown-item">
                                    <i class="bi bi-box-arrow-right me-2"></i>Sair
                                </button>
                            </form>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>