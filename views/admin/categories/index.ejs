<%- include('../../partials/header.ejs') %>
<%- include('../../partials/navbar.ejs') %>

<body>
    <main class="main-content">
        <div class="container">
            <div id="error-popup-container"></div>
            <div class="d-flex justify-content-between align-items-center my-4">
                <h2 class="mb-0 h4">
                    <i class="bi bi-tag-fill me-2"></i>
                    Gerenciamento de Categorias
                </h2>
                <a href="/admin/categories/new" class="btn btn-primary">
                    <i class="bi bi-plus-circle me-2"></i>Nova Categoria
                </a>
            </div>

            <div class="card shadow-sm">
                <div class="card-body">
                    <div class="table-responsive">
                        <table id="table-data" class="table table-striped table-hover align-middle">
                            <thead class="table-light">
                                <tr>
                                    <th>Categorias</th>
                                    <th class="text-center">
                                        <div class="d-flex align-items-center justify-content-center">
                                            <span>Ações</span>
                                            <div class="form-check form-switch ms-3">
                                                <input aria-checked="false" class="form-check-input" type="checkbox" role="switch" id="toggleExcluir" title="Habilitar exclusão">
                                            </div>
                                        </div>
                                    </th>
                                </tr>
                            </thead>
                            <tbody id="category-table-body">
                                <% categories.forEach(category=> { %>
                                    <tr>
                                        <td><strong><%= category.nome %></strong></td>
                                        <td class="text-center">
                                            <div class="btn-group" role="group" aria-label="Ações da categoria">
                                                <a href="/admin/categories/edit/<%= category.id_categoria %>"
                                                    class="btn btn-sm btn-outline-primary" title="Editar categoria">
                                                    <i class="bi bi-pencil-square"></i>
                                                </a>
                                                <a href="/admin/produtos/categoria/<%= category.id_categoria %>"
                                                    class="btn btn-sm btn-outline-info" title="Ver produtos desta categoria">
                                                    <i class="bi bi-box-seam"></i>
                                                </a>
                                                <form data-delete-form action="/admin/categories/delete/<%= category.id_categoria %>" method="POST" class="d-inline-block">
                                                    <input type="hidden" name="id_categoria" value="<%= category.id_categoria %>">
                                                    <button type="submit" class="btn btn-sm btn-outline-danger excluir-btn" title="Excluir categoria" style="display: none;">
                                                        <i class="bi bi-trash"></i>
                                                    </button>
                                                </form>
                                            </div>
                                        </td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <%- include('../../partials/footer.ejs') %>

    <script type="module">
        import { setupRowDelete } from '/js/delete-handler.js';

        document.addEventListener('DOMContentLoaded', function () {
            const deleteForms = document.querySelectorAll('[data-delete-form]');
            const toggleExcluir = document.getElementById('toggleExcluir');
            const tableBody = document.getElementById('category-table-body');

            const tooltipTriggerList = Array.from(document.querySelectorAll('[title]')).filter(el => el.offsetParent !== null);
            const tooltipList = tooltipTriggerList.map(el => new bootstrap.Tooltip(el));


            setupRowDelete(deleteForms, toggleExcluir, tableBody);
        });
    </script>
</body>
</html>