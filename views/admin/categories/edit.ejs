<%- include('../../partials/header.ejs') %>
    <%- include('../../partials/navbar.ejs') %>

        <div class="container">
            <hr>
            <div class="card">
                <div class="card-header">
                    <h2>Edição de Categoria</h2>
                </div>
                <div class="card-body">
                    <form method="POST" action="/admin/categories/update/<%= category.id_categoria %>">
                        <input class="form-control" type="text" name="nome" placeholder="Defina o nome da categoria"
                            value="<%= category.nome %>"><br>
                        <input type="hidden" name="id_categoria" value="<%= category.id_categoria %>">
                        <button class="btn btn-success">Atualizar</button>
                    </form>
                </div>
            </div>
        </div>

        <%- include('../../partials/footer.ejs') %>