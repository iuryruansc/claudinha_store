<%- include('../../partials/header.ejs') %>
    <%- include('../../partials/navbar.ejs') %>

        <div class="container">
            <hr>
            <div class="card">
                <div class="card-header">
                    <h2>Edição de Estoque</h2>
                </div>
                <div class="card-body">
                    <form method="POST" action="/admin/estoques/update/<%= estoque.id_estoque %>">
                        <input class="form-control" type="text" name="quantidade_atual"
                            placeholder="Insira a quantidade atual de produtos."
                            value="<%= estoque.quantidade_atual %>"><br>
                        <input class="form-control" type="text" name="localizacao"
                            placeholder="Onde está localizado o produto?" value="<%= estoque.localizacao %>"><br>
                        </select><br>
                        <input type="hidden" name="id_estoque" value="<%= estoque.id_estoque %>">
                        <button class="btn btn-success">Atualizar</button>
                    </form>
                </div>
            </div>
        </div>

        <%- include('../../partials/footer.ejs') %>